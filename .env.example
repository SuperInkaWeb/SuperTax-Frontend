# URL del backend (plataforma-backend). En dev apunta directo a FastAPI.
VITE_API_URL=http://localhost:8000

# Auth0 (SPA). Rellena con los datos de tu tenant único para habilitar el login.
VITE_AUTH0_DOMAIN=
VITE_AUTH0_CLIENT_ID=
VITE_AUTH0_AUDIENCE=
